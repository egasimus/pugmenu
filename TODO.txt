// docs of different tools with chapters and outlines

// list of /dev/ptys with associated processes->shells->terminals->windows
// start with ps -at then sudo readlink /proc/???/cwd

// list of tmux sessions, spawn terminal with one
// list of git checkouts, spawn terminal in one

// powersaver mode:
// gksudo cpupower frequency-set -u 800 -d 800
// xbacklight slider

// sudo iw dev wlp3s0 scan
// sudo systemctl restart tor
// sudo systemctl restart mullvad-daemon
// sudo systemctl restart cups

